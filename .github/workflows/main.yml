name: Build Grok Mac App

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pyinstaller Pillow

      - name: Build with PyInstaller
        run: |
          # 1. Собираем приложение с твоими названиями файлов
          pyinstaller --noconfirm --onefile --windowed --add-data "grok.ico:." GrokPromptManager.py
          
          # 2. Переходим в папку с результатом
          cd dist
          
          # 3. Упаковываем в ZIP с сохранением прав доступа Mac (флаг -r)
          zip -r GrokPromptManager_MacOS.zip GrokPromptManager.app

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Grok-MacOS-Final
          path: dist/GrokPromptManager_MacOS.zip
